# Styles Previews

This directory is reserved for preview-specific style sources if the Design Genome later needs them.

`v0.5` keeps preview-specific styling inside the preview document itself so the reusable primitive source remains neutral and framework-agnostic.
